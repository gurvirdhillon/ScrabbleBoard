CREATE TABLE profile{
    firstname VARCHAR 15 NOT NULL,
    secondname VARCHAR 15 NOT NULL,
    username PRIMARY KEY NOT NULL UNIQUE,
    TIME DATETIME
}

INSERT INTO profile (firstname, secondname, username) VALUES(
    USER, LASTNAME, USERNAME_1
);
